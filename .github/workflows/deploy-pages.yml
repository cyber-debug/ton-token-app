name: Deploy beta to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  configuration:
    name: Check beta configuration
    runs-on: ubuntu-latest
    steps:
      - name: Check backend URL
        env:
          BETA_API_URL: ${{ vars.VITE_API_BASE_URL }}
        run: |
          if [ -z "$BETA_API_URL" ]; then
            echo "::notice::No VITE_API_BASE_URL is configured. Publishing the labeled demo with transfers disabled."
            exit 0
          fi
          node -e "const value = new URL(process.env.BETA_API_URL); if (value.protocol !== 'https:') throw new Error('VITE_API_BASE_URL must use HTTPS');"

  build:
    name: Build beta frontend
    needs: configuration
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      APP_PUBLIC_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/
      VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
      VITE_TON_NETWORK: ${{ vars.VITE_TON_NETWORK || 'testnet' }}
      VITE_DEMO_MODE: ${{ vars.VITE_API_BASE_URL == '' && 'true' || 'false' }}
      VITE_ROUTER_MODE: hash
      VITE_TONCONNECT_MANIFEST_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/tonconnect-manifest.json

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Lint and test
        run: npm run lint && npm test

      - name: Generate deployment manifest
        run: npm run manifest:generate

      - name: Build beta
        run: npm run build

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build

  deploy:
    name: Deploy beta frontend
    needs: build
    if: needs.build.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
